# ==========================================
# 本地開發模式環境變數
# ==========================================
# 此配置用於本地開發，連接到 Docker 的資料庫和 Redis
# 不會影響 Docker 測試環境的配置
# ==========================================

NODE_ENV=development

# 連接到 Docker 的資料庫（端口 15432）
PG_DATABASE_URL=postgres://postgres:test_password_local@localhost:15432/default

# 連接到 Docker 的 Redis（端口 16379）
REDIS_URL=redis://localhost:16379

# 應用密鑰
APP_SECRET=a_super_secret_random_string_for_jwt

# 自動填充登入資訊（開發用）
SIGN_IN_PREFILLED=true

# 前端 URL（本地開發）
FRONTEND_URL=http://localhost:3001

# LINE 加密金鑰（與 Docker 環境相同，確保資料可互通）
LINE_CONFIG_ENCRYPTION_KEY=dc48fd05bd947bb5d296dd8ed88b200a73bab4e00e59ffe2d5e81b55bdc12c24

# 伺服器 URL
SERVER_URL=http://localhost:3000

# 單租戶模式
IS_MULTIWORKSPACE_ENABLED=false
DEFAULT_SUBDOMAIN=app

# 其他配置
IS_BILLING_ENABLED=false
STORAGE_TYPE=local
LOG_LEVEL=debug
