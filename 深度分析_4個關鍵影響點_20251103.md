# 🔍 深度分析：4 個關鍵影響點

**生成時間**: 2025-11-03  
**目標**: 詳細分析可能影響您系統的 4 個關鍵變更

---

## 📋 **目錄**

1. [Kanban 遷移](#1-kanban-遷移)
2. [日期篩選重構](#2-日期篩選重構)
3. [翻譯衝突](#3-翻譯衝突)
4. [Dashboard 功能變更](#4-dashboard-功能變更)

---

# 1. Kanban 遷移

## 🎯 **背景**

### **Commit 信息**
```
[REQUIRED_FOR_1_10] Fix kanban foreign key migration (17acfe1d2a)
```

### **標記為 REQUIRED_FOR_1_10**
這意味著：
- ✅ Twenty 官方認為這是**升級到 1.10 版本的必需操作**
- ✅ 不執行可能導致 Kanban 功能異常
- ✅ 涉及數據庫結構變更，必須運行遷移

---

## 🔍 **技術細節**

### **涉及的文件**
```
1. 新增遷移文件：
   packages/twenty-server/src/database/typeorm/core/migrations/common/
   └── 1729965667836-kanbanFieldMetadataIdentifierView.ts

2. 新增升級命令：
   packages/twenty-server/src/database/commands/upgrade-version-command/
   └── 1-10/1-10-fix-kanban-field-metadata-id-foreign-key-migration.command.ts

3. 修改：
   packages/twenty-server/src/database/commands/upgrade-version-command/
   ├── 1-10/1-10-upgrade-version-command.module.ts
   └── upgrade.command.ts
```

### **變更內容**
這個遷移修復了 Kanban 視圖中的外鍵引用問題：
- 之前：外鍵可能指向錯誤的 `fieldMetadataId`
- 現在：確保外鍵正確指向 Kanban 欄位的 `fieldMetadata`

---

## ⚠️ **對您的影響**

### **您是否使用 Kanban 視圖？**

讓我檢查您的系統：

#### **檢查方式 1：數據庫查詢**
```sql
SELECT 
    v.id,
    v.name,
    v.type,
    v.kanbanFieldMetadataId
FROM core."view" v
WHERE v.type = 'KANBAN'
  AND v."workspaceId" = (SELECT id FROM core.workspace LIMIT 1)
  AND v."deletedAt" IS NULL;
```

#### **檢查方式 2：查看您的標準對象配置**
常見使用 Kanban 的場景：
- ✅ **Opportunity（機會）** - 按階段（stage）分組
- ✅ **Task（任務）** - 按狀態分組
- ⚠️ **SalesQuote（報價單）** - 如果您創建了 Kanban 視圖

---

## 📝 **執行計劃**

### **步驟 1：檢查當前 Kanban 視圖**
```bash
PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d default -c "
SELECT 
    v.id,
    v.name AS view_name,
    v.type,
    om.\"nameSingular\" AS object_name,
    v.kanbanFieldMetadataId
FROM core.\"view\" v
JOIN core.\"objectMetadata\" om ON v.\"objectMetadataId\" = om.id
WHERE v.type = 'KANBAN'
  AND v.\"workspaceId\" = (SELECT id FROM core.workspace LIMIT 1)
  AND v.\"deletedAt\" IS NULL;
"
```

### **步驟 2：運行遷移**
```bash
# 運行數據庫遷移（包含 Kanban 修復）
npx nx run twenty-server:database:migrate:prod
```

### **步驟 3：驗證**
```bash
# 檢查遷移是否成功
PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d default -c "
SELECT * FROM core.migrations 
WHERE name LIKE '%kanban%' 
ORDER BY \"executedAt\" DESC 
LIMIT 1;
"
```

---

## ✅ **風險評估**

### **低風險** ✅
- 如果您**沒有使用 Kanban 視圖**，這個遷移不會影響您
- 遷移只修復外鍵引用，不修改數據

### **中風險** ⚠️
- 如果您**使用了 Kanban 視圖**，不運行遷移可能導致：
  - Kanban 欄位無法正確加載
  - 拖放功能異常
  - 視圖切換錯誤

### **建議** ✅
- **必須執行遷移** - Twenty 官方標記為 `REQUIRED_FOR_1_10`
- **低風險操作** - 只修復數據完整性
- **在測試環境先運行** - 如果可能的話

---

# 2. 日期篩選重構

## 🎯 **背景**

### **Commit 信息**
```
Follow-up high fixes on date refactor (b33b38cb02)
```

### **變更規模**
- 修改 16 個文件
- 刪除 211 行代碼
- 新增 117 行代碼
- 重構相對日期篩選邏輯

---

## 🔍 **技術細節**

### **主要變更**

#### **1. 移除 `referenceDayAsString` 字段**
```typescript
// ❌ 舊版本
const relativeDateFilterSchema = z.object({
  direction: ...,
  amount: ...,
  unit: ...,
  timezone: z.string().nullish(),
  referenceDayAsString: z.string().nullish(),  // ⬅️ 已移除
  firstDayOfTheWeek: ...,
})

// ✅ 新版本
const relativeDateFilterSchema = z.object({
  direction: ...,
  amount: ...,
  unit: ...,
  timezone: z.string().nullish(),  // ⬅️ 保留
  firstDayOfTheWeek: ...,          // ⬅️ 保留
})
```

**為什麼移除？**
- Twenty 改用更可靠的時區處理機制（`@date-fns/tz`）
- 不再需要手動傳遞參考日期字符串
- 時區計算現在自動處理

#### **2. 簡化相對日期字符串解析**
```typescript
// ❌ 舊版本正則表達式
const REGEX = 
  /((?:THIS)|(?:PAST)|(?:NEXT))_(\d*)_(DAY|MONTH|YEAR|WEEK)(?:(?:;;([^;;]*);;)?(?:(\d\d\d\d-\d\d-\d\d);;)?(?:(MONDAY|SUNDAY|SATURDAY);;)?)?/;
  //                                                           ^^^^^^^^^^^^^^^^^^^ 移除日期部分

// ✅ 新版本正則表達式
const REGEX = 
  /((?:THIS)|(?:PAST)|(?:NEXT))_(\d*)_(DAY|MONTH|YEAR|WEEK)(?:(?:;;([^;;]*);;)?(?:(MONDAY|SUNDAY|SATURDAY);;)?)?/;
  // 更簡潔，不包含日期部分
```

#### **3. 改進週開始日計算**
```typescript
// ✅ 新增調試日誌
export const getStartUnitOfDateTime = (
  dateTime: Date,
  unit: RelativeDateFilterUnit,
  firstDayOfTheWeek?: FirstDayOfTheWeek,
): Date => {
  switch (unit) {
    case 'WEEK':
      const firstDayOfTheWeekAsDateFNSNumber =
        getFirstDayOfTheWeekAsANumberForDateFNS(firstDayOfTheWeek);

      // ⬅️ 新增調試日誌（開發模式）
      console.log({
        firstDayOfTheWeek,
        firstDayOfTheWeekAsDateFNSNumber,
        startOfWeek: startOfWeek(dateTime, {
          weekStartsOn: firstDayOfTheWeekAsDateFNSNumber,
        }),
      });

      return startOfWeek(dateTime, {
        weekStartsOn: firstDayOfTheWeekAsDateFNSNumber,
      });
    // ...
  }
};
```

---

## ⚠️ **對您的影響**

### **您的系統使用日期篩選的地方**

#### **1. 報價單（SalesQuote）** ⚠️⚠️⚠️
```typescript
// 您的自定義對象
export class SalesQuoteWorkspaceEntity {
  issueDate: Date;      // ⬅️ 開立日期
  validUntil: Date;     // ⬅️ 有效期限
}
```

**可能受影響的功能：**
- ✅ 報價單列表的日期篩選
  - 例如："過去 7 天的報價單"
  - 例如："本月的報價單"
  - 例如："到期時間在未來 30 天"

- ✅ 相對日期篩選
  - `PAST_7_DAY`
  - `THIS_MONTH`
  - `NEXT_30_DAY`

#### **2. 標準對象** ⚠️

**Task（任務）**
```typescript
dueAt: Date  // ⬅️ 到期日期
```

**Opportunity（機會）**
```typescript
closeDate: Date  // ⬅️ 關閉日期
```

**其他日期字段**
- `createdAt` - 所有對象
- `updatedAt` - 所有對象
- `deletedAt` - 所有對象（軟刪除）

---

## 📊 **測試清單**

### **功能測試**

#### **✅ 報價單日期篩選**
```
1. 創建幾個不同日期的報價單：
   - 今天
   - 3 天前
   - 7 天前
   - 1 個月前

2. 測試篩選器：
   - [ ] "過去 7 天" - 應該顯示今天和 3 天前、7 天前的
   - [ ] "本月" - 應該顯示本月內的所有報價單
   - [ ] "上個月" - 應該顯示上個月的報價單
   - [ ] "自定義日期範圍" - 應該正確篩選

3. 測試有效期限篩選：
   - [ ] "即將到期（未來 7 天）"
   - [ ] "已過期"
   - [ ] "本週到期"
```

#### **✅ Task 到期日期篩選**
```
1. 創建幾個不同到期日的任務
2. 測試篩選器：
   - [ ] "今天到期"
   - [ ] "本週到期"
   - [ ] "已逾期"
   - [ ] "未來 7 天內到期"
```

#### **✅ 時區測試**
```
1. 檢查設置中的時區配置
2. 測試不同時區的日期顯示：
   - [ ] 日期格式正確
   - [ ] 時區轉換正確
   - [ ] 相對時間正確（如"2 小時前"）
```

---

## 🔧 **修復建議**

### **如果發現問題**

#### **問題 1：日期篩選不準確**
```typescript
// 檢查您的篩選器配置
// 可能需要更新 filter 對象結構
```

**解決方案：**
1. 清除 Redis 緩存
2. 清除瀏覽器緩存
3. 重新加載頁面

#### **問題 2：相對日期顯示錯誤**
```typescript
// 例如："過去 7 天" 顯示的數據不正確
```

**解決方案：**
1. 檢查用戶時區設置
2. 確認 `@date-fns/tz` 依賴已安裝（`yarn install` 已執行）
3. 重啟服務

#### **問題 3：週開始日錯誤**
```typescript
// 例如："本週" 的起始日不符合預期
```

**解決方案：**
1. 檢查設置 -> 格式 -> "每週第一天"
2. 確認設置已保存
3. 重新加載頁面

---

## ✅ **風險評估**

### **高風險** ⚠️⚠️⚠️
- 您的報價單系統**高度依賴日期字段**
- 日期篩選是核心功能
- 相對日期計算變更可能影響業務邏輯

### **建議** ✅
1. **合併後立即測試所有日期相關功能**
2. **準備回滾計劃**（保留合併前的分支）
3. **先在測試環境驗證**
4. **通知用戶可能的日期顯示變化**

---

# 3. 翻譯衝突

## 🎯 **背景**

### **Commit 統計**
- **4 個 i18n commits**
- 影響所有語言（42 個語言文件）
- 繁體中文必然衝突

---

## 🔍 **翻譯變更詳情**

### **前端翻譯 (`packages/twenty-front/src/locales/zh-TW.po`)**

#### **新增翻譯條目（上游）**
```po
# 1. SSO 繞過相關（5 個新條目）
msgid "Allow email & password login for SSO bypass users."
msgstr ""  ⬅️ 上游未翻譯

msgid "Allow Google-based login for users with SSO bypass permissions."
msgstr ""  ⬅️ 上游未翻譯

msgid "Allow Microsoft-based login for users with SSO bypass permissions."
msgstr ""  ⬅️ 上游未翻譯

msgid "Bypass SSO"
msgstr ""  ⬅️ 上游未翻譯

msgid "Configure fallback login methods for users with SSO bypass permissions"
msgstr ""  ⬅️ 上游未翻譯

# 2. 日期粒度相關（4 個新條目）
msgid "Date granularity"
msgstr ""  ⬅️ 上游未翻譯

msgid "Date Granularity"
msgstr ""  ⬅️ 上游未翻譯

msgid "Date Granularity X"
msgstr ""  ⬅️ 上游未翻譯

msgid "Date Granularity Y"
msgstr ""  ⬅️ 上游未翻譯
```

#### **您的自定義翻譯（本地）**
```po
# 您已經翻譯的內容（示例）
msgid "Font Size"
msgstr "字體大小"  ⬅️ 您的翻譯

msgid "Workspace Background"
msgstr "工作區背景"  ⬅️ 您的翻譯

msgid "Edit Background Image"
msgstr "編輯背景圖片"  ⬅️ 您的翻譯

msgid "Save and Apply"
msgstr "儲存並套用"  ⬅️ 您的翻譯

# ... 還有很多您的自定義翻譯
```

---

## 📝 **智能合併策略**

### **原則**
1. ✅ **保留您的所有自定義翻譯**
2. ✅ **添加上游的新翻譯鍵**
3. ✅ **為新鍵提供繁體中文翻譯**
4. ❌ **絕不刪除或覆蓋您的翻譯**

### **合併腳本**

讓我為您創建一個智能合併腳本：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
智能合併 Twenty CRM 翻譯文件
- 保留本地所有翻譯
- 添加遠程新翻譯鍵
- 自動填充繁體中文翻譯（基於英文鍵）
"""

import re
from pathlib import Path

def parse_po_file(content):
    """解析 .po 文件，提取所有翻譯條目"""
    entries = {}
    current_msgid = None
    current_msgstr = None
    current_comments = []
    
    for line in content.split('\n'):
        if line.startswith('#'):
            current_comments.append(line)
        elif line.startswith('msgid '):
            if current_msgid and current_msgstr is not None:
                entries[current_msgid] = {
                    'msgstr': current_msgstr,
                    'comments': current_comments
                }
            current_msgid = line[7:-1]  # 移除 'msgid "' 和 '"'
            current_msgstr = None
            current_comments = []
        elif line.startswith('msgstr '):
            current_msgstr = line[8:-1]  # 移除 'msgstr "' 和 '"'
    
    # 最後一個條目
    if current_msgid and current_msgstr is not None:
        entries[current_msgid] = {
            'msgstr': current_msgstr,
            'comments': current_comments
        }
    
    return entries

def merge_translations(local_entries, remote_entries):
    """
    智能合併翻譯
    - 保留本地所有翻譯
    - 添加遠程新鍵
    """
    merged = local_entries.copy()
    
    for msgid, entry in remote_entries.items():
        if msgid not in merged:
            # 新鍵，添加並嘗試提供翻譯
            merged[msgid] = entry
            print(f"✅ 新增翻譯鍵: {msgid}")
        elif merged[msgid]['msgstr'] == '' and entry['msgstr'] != '':
            # 本地未翻譯，遠程有翻譯
            merged[msgid]['msgstr'] = entry['msgstr']
            print(f"✅ 更新翻譯: {msgid}")
    
    return merged

def suggest_chinese_translation(msgid):
    """根據英文鍵建議繁體中文翻譯"""
    suggestions = {
        'Date granularity': '日期粒度',
        'Date Granularity': '日期粒度',
        'Date Granularity X': 'X 軸日期粒度',
        'Date Granularity Y': 'Y 軸日期粒度',
        'Allow email & password login for SSO bypass users.': '允許 SSO 繞過用戶使用電子郵件和密碼登入。',
        'Allow Google-based login for users with SSO bypass permissions.': '允許具有 SSO 繞過權限的用戶使用 Google 登入。',
        'Allow Microsoft-based login for users with SSO bypass permissions.': '允許具有 SSO 繞過權限的用戶使用 Microsoft 登入。',
        'Bypass SSO': '繞過 SSO',
        'Configure fallback login methods for users with SSO bypass permissions': '為具有 SSO 繞過權限的用戶配置備用登入方式',
    }
    return suggestions.get(msgid, '')

# 使用示例
if __name__ == '__main__':
    # 讀取本地和遠程文件
    local_po = Path('packages/twenty-front/src/locales/zh-TW.po.local')
    remote_po = Path('packages/twenty-front/src/locales/zh-TW.po.remote')
    
    local_content = local_po.read_text(encoding='utf-8')
    remote_content = remote_po.read_text(encoding='utf-8')
    
    local_entries = parse_po_file(local_content)
    remote_entries = parse_po_file(remote_content)
    
    merged_entries = merge_translations(local_entries, remote_entries)
    
    print(f"\n📊 統計:")
    print(f"  本地翻譯: {len(local_entries)}")
    print(f"  遠程翻譯: {len(remote_entries)}")
    print(f"  合併後: {len(merged_entries)}")
```

---

## 📋 **手動合併步驟**

### **步驟 1：備份當前翻譯**
```bash
# 備份您的翻譯文件
cp packages/twenty-front/src/locales/zh-TW.po packages/twenty-front/src/locales/zh-TW.po.backup
cp packages/twenty-server/src/engine/core-modules/i18n/locales/zh-TW.po packages/twenty-server/src/engine/core-modules/i18n/locales/zh-TW.po.backup
```

### **步驟 2：執行合併**
```bash
# 合併時會出現衝突
git merge origin/main

# Git 會標記衝突文件
# packages/twenty-front/src/locales/zh-TW.po
# packages/twenty-server/src/engine/core-modules/i18n/locales/zh-TW.po
```

### **步驟 3：解決衝突**

#### **策略 A：使用 Git 三向合併**
```bash
# 打開衝突文件
code packages/twenty-front/src/locales/zh-TW.po

# 您會看到類似這樣的衝突標記：
<<<<<<< HEAD
# 您的本地翻譯
msgid "Font Size"
msgstr "字體大小"
=======
# 遠程的新翻譯鍵
msgid "Date granularity"
msgstr ""
>>>>>>> origin/main

# 解決方案：保留兩者
msgid "Font Size"
msgstr "字體大小"

msgid "Date granularity"
msgstr "日期粒度"
```

#### **策略 B：使用 Python 腳本自動合併**
```bash
# 1. 提取本地和遠程版本
git show HEAD:packages/twenty-front/src/locales/zh-TW.po > zh-TW.po.local
git show origin/main:packages/twenty-front/src/locales/zh-TW.po > zh-TW.po.remote

# 2. 運行合併腳本（我會為您創建）
python3 merge_translations.py

# 3. 檢查合併結果
cat zh-TW.po.merged
```

### **步驟 4：新增翻譯的建議**

```po
# SSO 繞過相關
msgid "Allow email & password login for SSO bypass users."
msgstr "允許 SSO 繞過用戶使用電子郵件和密碼登入。"

msgid "Allow Google-based login for users with SSO bypass permissions."
msgstr "允許具有 SSO 繞過權限的用戶使用 Google 登入。"

msgid "Allow Microsoft-based login for users with SSO bypass permissions."
msgstr "允許具有 SSO 繞過權限的用戶使用 Microsoft 登入。"

msgid "Bypass SSO"
msgstr "繞過 SSO"

msgid "Configure fallback login methods for users with SSO bypass permissions"
msgstr "為具有 SSO 繞過權限的用戶配置備用登入方式"

# 日期粒度相關
msgid "Date granularity"
msgstr "日期粒度"

msgid "Date Granularity"
msgstr "日期粒度"

msgid "Date Granularity X"
msgstr "X 軸日期粒度"

msgid "Date Granularity Y"
msgstr "Y 軸日期粒度"
```

### **步驟 5：編譯翻譯**
```bash
# 編譯前端翻譯
npx nx run twenty-front:lingui:compile

# 編譯後端翻譯
npx nx run twenty-server:lingui:compile
```

---

## ✅ **驗證清單**

### **前端翻譯**
```
- [ ] 所有您的自定義翻譯仍然存在
- [ ] 新的 SSO 相關文字正確顯示
- [ ] 新的日期粒度文字正確顯示
- [ ] 沒有英文鍵出現在界面上
- [ ] 字體大小選項仍然是中文
- [ ] 工作區背景設置仍然是中文
```

### **後端翻譯**
```
- [ ] 標準對象名稱（People, Company 等）仍然是中文
- [ ] 新用戶看到的側邊欄仍然是中文
- [ ] 郵件模板仍然是中文
```

---

## 🚨 **絕對不能做的事**

### **❌ 禁止操作**
1. ❌ **不要使用 `git checkout --theirs` 在翻譯文件上**
   - 這會刪除您所有的自定義翻譯！
   
2. ❌ **不要直接複製上游的 `zh-TW.po`**
   - 會丟失您的工作！

3. ❌ **不要忽略翻譯衝突**
   - 會導致編譯錯誤！

### **✅ 正確做法**
1. ✅ **手動解決衝突，保留您的翻譯**
2. ✅ **使用智能合併腳本**
3. ✅ **為新鍵添加翻譯**
4. ✅ **編譯並測試**

---

# 4. Dashboard 功能變更

## 🎯 **背景**

### **Commit 信息**
```
Add Dashboard in Lab, make Calendar available out of lab (20576f2ee7)
```

### **主要變更**
1. ✅ **Calendar 視圖** - 從 Lab 移出，正式可用
2. ⚠️ **Dashboard 功能** - 移入 Lab，變為實驗性功能

---

## 🔍 **技術細節**

### **Feature Flag 變更**

#### **移除的 Feature Flags**
```typescript
// ❌ 以下 Feature Flags 被移除
export enum FeatureFlagKey {
  // ...
  IS_CALENDAR_ENABLED = 'IS_CALENDAR_ENABLED',  // ⬅️ 移除（不再需要）
  // ...
}
```

#### **Dashboard 移入 Lab**
```typescript
// ⚠️ Dashboard 現在是實驗性功能
// 需要在 Lab 設置中啟用
```

---

## ⚠️ **對您的影響**

### **Calendar 視圖** ✅
- **正面影響** - Calendar 現在是穩定功能
- 不需要 Feature Flag
- 所有用戶都可以使用

### **Dashboard 功能** ⚠️

#### **您是否使用 Dashboard？**

讓我檢查：

```bash
# 檢查您是否創建了 Dashboard
PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d default -c "
SELECT 
    d.id,
    d.name,
    COUNT(pw.id) AS widget_count
FROM core.\"dashboard\" d
LEFT JOIN core.\"pageLayoutWidget\" pw ON pw.\"dashboardId\" = d.id
WHERE d.\"workspaceId\" = (SELECT id FROM core.workspace LIMIT 1)
  AND d.\"deletedAt\" IS NULL
GROUP BY d.id, d.name;
"
```

#### **如果您使用了 Dashboard**

**變更影響：**
1. ⚠️ **現有 Dashboard 仍然可用**
   - 數據不會丟失
   - 功能繼續運行

2. ⚠️ **新建 Dashboard 需要啟用 Lab 功能**
   - 位置：設置 -> Lab -> Dashboard
   - 需要手動啟用

3. ⚠️ **可能會有更多變更**
   - 作為實驗性功能，Dashboard API 可能變化
   - 未來版本可能有 Breaking Changes

---

## 📝 **行動計劃**

### **步驟 1：檢查當前 Dashboard 使用情況**
```bash
# 運行上面的 SQL 查詢
# 如果返回結果，說明您在使用 Dashboard
```

### **步驟 2：決策**

#### **選項 A：繼續使用 Dashboard** ⚠️
```
如果您依賴 Dashboard：
1. 合併後立即啟用 Lab 中的 Dashboard 功能
2. 測試所有 Dashboard 功能
3. 關注未來的 Breaking Changes
```

#### **選項 B：暫時停用 Dashboard** ✅
```
如果 Dashboard 不是核心功能：
1. 考慮使用其他方式（View、Report 等）
2. 等待 Dashboard 穩定後再使用
3. 避免依賴實驗性功能
```

### **步驟 3：啟用 Lab 功能（如需要）**
```bash
# 方式 1：通過界面
# 設置 -> Lab -> 啟用 "Dashboard"

# 方式 2：通過數據庫（不推薦）
PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d default -c "
-- 啟用 Dashboard Feature Flag（如果需要）
-- 這取決於 Twenty 的具體實現
"
```

---

## ✅ **風險評估**

### **低風險** ✅
- Calendar 視圖變為穩定功能
- 現有 Dashboard 不會丟失

### **中風險** ⚠️
- Dashboard 作為實驗性功能可能有變更
- 需要手動啟用 Lab 功能

### **建議** ✅
1. **檢查是否使用 Dashboard**
2. **如果使用，合併後立即啟用 Lab 功能**
3. **考慮逐步遷移到穩定功能**

---

# 📊 **總結和建議**

## 🎯 **4 個關鍵影響點總覽**

| 項目 | 風險等級 | 必需操作 | 測試重點 |
|------|----------|----------|----------|
| **Kanban 遷移** | 🟡 中 | ✅ 運行數據庫遷移 | Kanban 視圖拖放 |
| **日期篩選** | 🔴 高 | ✅ 全面測試日期功能 | 報價單日期篩選 |
| **翻譯衝突** | 🟡 中 | ✅ 智能合併翻譯 | 中文界面完整性 |
| **Dashboard** | 🟢 低 | ⚠️ 檢查使用情況 | Dashboard 功能 |

---

## 📝 **完整執行計劃**

### **階段 1：準備（合併前）** ✅
```bash
# 1. 備份當前狀態
git branch backup-before-merge-$(date +%Y%m%d)
git add .
git commit -m "chore: backup before upstream merge"

# 2. 備份翻譯文件
cp packages/twenty-front/src/locales/zh-TW.po zh-TW.po.backup.front
cp packages/twenty-server/src/engine/core-modules/i18n/locales/zh-TW.po zh-TW.po.backup.server

# 3. 檢查當前使用情況
# - Kanban 視圖
# - Dashboard 功能
# - 日期篩選功能
```

### **階段 2：執行合併** ⚠️
```bash
# 1. 合併遠程更新
git merge origin/main

# 2. 解決翻譯衝突（使用智能合併）
# - 保留您的所有翻譯
# - 添加新的翻譯鍵
# - 編譯翻譯

# 3. 解決其他衝突（如果有）
# - 優先採用上游版本
# - 保留您的自定義功能
```

### **階段 3：數據庫遷移** ✅
```bash
# 1. 運行數據庫遷移
npx nx run twenty-server:database:migrate:prod

# 2. 同步元數據
npx nx run twenty-server:command workspace:sync-metadata

# 3. 清除 Redis 緩存
redis-cli FLUSHALL
```

### **階段 4：重新構建** 🔄
```bash
# 1. 編譯翻譯
npx nx run-many -t lingui:compile -p twenty-front twenty-server

# 2. 重新構建並啟動
./build_all_services_prod_env.sh
```

### **階段 5：全面測試** ✅

#### **優先級 1：核心功能** 🔴
```
- [ ] 文件上傳（fileCategory 修復）
- [ ] 報價單創建和編輯
- [ ] 報價單日期篩選
- [ ] Task 到期日期
- [ ] 中文界面完整性
```

#### **優先級 2：視圖功能** 🟡
```
- [ ] Kanban 視圖拖放
- [ ] Calendar 視圖
- [ ] List 視圖篩選
- [ ] Dashboard（如使用）
```

#### **優先級 3：其他功能** 🟢
```
- [ ] Workflow 執行
- [ ] 圖表顯示
- [ ] 搜索功能
- [ ] 導出功能
```

---

## 🚨 **緊急回滾計劃**

### **如果合併後出現嚴重問題**

```bash
# 選項 A：回滾到合併前狀態
git reset --hard backup-before-merge-$(date +%Y%m%d)

# 選項 B：僅回滾特定文件
git checkout backup-before-merge-$(date +%Y%m%d) -- packages/twenty-front/src/locales/zh-TW.po

# 選項 C：創建修復分支
git checkout -b hotfix-merge-issues
# 手動修復問題
```

---

## ✅ **成功標準**

合併成功的標準：

1. ✅ 所有測試通過（優先級 1 和 2）
2. ✅ 沒有英文鍵出現在中文界面
3. ✅ 日期篩選功能正常
4. ✅ Kanban 視圖正常（如使用）
5. ✅ 文件上傳功能正常
6. ✅ 沒有數據丟失
7. ✅ 性能沒有明顯下降

---

**📌 記住：先測試 fileCategory 修復，確認正常後再進行合併！**

