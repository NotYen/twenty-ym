# Docker 建置版本統一管理
# 統一使用 Alpine 以減少映像大小並與官方 Twenty 保持一致
NODE_VERSION=24-bookworm
NGINX_VERSION=1.27-alpine
POSTGRES_VERSION=16-alpine
REDIS_VERSION=7-alpine

# 應用版本
BACKEND_IMAGE_VERSION=dev
FRONTEND_IMAGE_VERSION=dev
